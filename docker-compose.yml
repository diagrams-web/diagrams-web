version: "3"
services:
  web:
    build: ./web
    ports: ["5000:5000"]
    volumes: ['./web:/app']
    command: ["/tmp/startup.sh"]
    healthcheck:
      test: curl --fail -s http://localhost:5000/ || exit 1
      interval: 1m30s
      timeout: 10s
      retries: 3
